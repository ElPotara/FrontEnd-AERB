function obtenerInfo(name = 'Angel', apellido = 'Rosales'){
    const info = `${name} ${apellido}`;       

    return info;
}       

const info = obtenerInfo('Eduardo', 'Banda');

console.log(info)

